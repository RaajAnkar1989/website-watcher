/**
 * Copy this file to auth.config.js and fill in your token.
 *
 * SECURITY:
 * - auth.config.js is gitignored — safe for local use with python3 serve.py
 * - If you commit a token to GitHub (including in this file), anyone can steal it
 * - For GitHub Pages on your phone, prefer Settings once (token stays in browser)
 */
window.WATCHBOARD_AUTH = {
  owner: 'RaajAnkar1989',
  repo: 'website-watcher',
  // token: 'github_pat_ paste your token here',
};
